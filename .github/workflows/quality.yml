name: Site quality

on:
  pull_request:
    branches:
      - clean-blog-v1
  push:
    branches:
      - clean-blog-v1

permissions:
  contents: write

concurrency:
  group: site-quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout pull request branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref || github.ref_name }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Pin patched PostCSS and Sharp transitive releases
        if: github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository
        shell: bash
        run: |
          npm pkg set overrides.postcss=8.5.22 overrides.sharp=0.35.3
          npm install
          npm ls postcss sharp
          if git diff --quiet -- package.json package-lock.json; then
            echo "Dependency overrides are already current."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add package.json package-lock.json
            git commit -m "Override vulnerable PostCSS and Sharp releases"
            git push origin HEAD:${{ github.event.pull_request.head.ref }}
          fi

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high

      - name: Run complete production quality gate
        id: quality-gate
        shell: bash
        run: |
          set +e
          npm run build > quality-build.log 2>&1
          status=$?
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Upload quality build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: site-quality-build-log
          path: quality-build.log
          retention-days: 14

      - name: Print failed build log tail
        if: steps.quality-gate.outputs.status != '0'
        run: tail -n 240 quality-build.log

      - name: Fail when quality gate failed
        if: steps.quality-gate.outputs.status != '0'
        run: exit 1

      - name: Print verification backlog
        if: steps.quality-gate.outputs.status == '0'
        run: npm run verification:report
